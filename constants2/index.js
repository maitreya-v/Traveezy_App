import icons from './icons';
import images from './images';
import theme, {COLORS, SIZES, FONTS, styles} from '../constants2/theme';
import {optionItems, destinations} from './data';

export {
  icons,
  images,
  theme,
  optionItems,
  destinations,
  COLORS,
  SIZES,
  FONTS,
  styles,
};
